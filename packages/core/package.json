{
  "name": "@nexora/core",
  "version": "1.9.0",
  "description": "Shared pure helpers and types for Nexora web + mobile — single source of truth for format helpers, file-type detection, and transcode codec tables",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vitest": "^3.2.0"
  }
}
